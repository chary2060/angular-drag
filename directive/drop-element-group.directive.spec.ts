import { DropElementGroupDirective } from './drop-element-group.directive';

describe('DropElementGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new DropElementGroupDirective();
    expect(directive).toBeTruthy();
  });
});
