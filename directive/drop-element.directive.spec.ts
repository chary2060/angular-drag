import { DropElementDirective } from './drop-element.directive';

describe('DropElementDirective', () => {
  it('should create an instance', () => {
    const directive = new DropElementDirective();
    expect(directive).toBeTruthy();
  });
});
